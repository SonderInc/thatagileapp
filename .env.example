# Firebase config (from Firebase Console → Project settings → Your apps)
# Copy to .env.local and fill in values. Do not commit .env.local.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Optional: for Firebase Analytics (get from same config in Firebase Console).
# Set VITE_FIREBASE_ANALYTICS_ENABLED=true to enable; otherwise gtag is not loaded (avoids ERR_NAME_NOT_RESOLVED when blocked).
VITE_FIREBASE_MEASUREMENT_ID=
VITE_FIREBASE_ANALYTICS_ENABLED=

# Optional: for Google Analytics 4 (gtag.js). Use id= format, e.g. G-XXXXXXXXXX
VITE_GA_MEASUREMENT_ID=
