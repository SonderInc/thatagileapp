# On-prem / air-gapped. Copy to .env and edit. No internet required after images are loaded.

# Deployment mode: saas | onprem | airgapped
DEPLOYMENT_MODE=onprem

# Port for the app (proxy)
APP_PORT=8080

# Base URL users will use (e.g. http://localhost:8080 or https://internal.company.com)
VITE_APP_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=

# Providers (phase 1 onprem can still use firebase if network allows; or local/postgres later)
VITE_AUTH_PROVIDER=firebase
VITE_DATA_PROVIDER=firestore
VITE_STORAGE_PROVIDER=firebase

# Tenant id for single-tenant
VITE_TENANT_ID=

# Optional: Firebase (only if AUTH_PROVIDER=firebase and network allows)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=

# API image (when backend exists); for now placeholder
API_IMAGE=busybox:1.36
